name: test

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  toolkit:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.24.2"
          cache-dependency-path: toolkit/go.sum
      - name: Install native build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential libgmp-dev libssl-dev
      - name: Build, test, and verify
        run: make test
      - name: Check public package hygiene
        run: make check-public
      - name: Check source manifest is current
        run: |
          make manifest
          git diff --exit-code -- manifests/source-code.sha256
