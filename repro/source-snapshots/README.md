# Historical source snapshots

The toolkit commands evolved as additional claim components and cutoff-aware
checks were added. This directory preserves the exact source files copied from
the September 6 and September 8 run directories.

Use `toolkit/` for the maintained implementation. Use these snapshots only to
reconstruct the historical run or compare source hashes.

Compiled binaries are intentionally omitted from Git.
